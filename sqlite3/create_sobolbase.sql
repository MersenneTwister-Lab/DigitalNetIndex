drop table sobolbase;
create table sobolbase (
	name text,
        bit int,
        s int,
        m int,
        wafom real,
        tvalue int,
        data text,
        primary key (name, bit, s, m));
